import React from "react";

const AuthLayout = ({ children }: { children: React.ReactNode }) => {
  return <>{children}</>; // you can wrap this in a styled div if needed
};

export default AuthLayout;
